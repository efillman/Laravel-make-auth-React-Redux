import React from 'react';
import { Navbar, Row, Table, Col } from 'react-bootstrap';
import {Link} from 'react-router-dom';

const Footer = () => (
    <footer className={'footer'}>
    <Navbar>
          <Row>
          </Row>
    </Navbar>
    </footer>
);

export default Footer;
